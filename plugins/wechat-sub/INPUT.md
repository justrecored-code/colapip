# wechat-sub 输入规范

## 任务参数
- action: string (必需) — search | subscribe | unsubscribe | poll | list | fetch_all_history
- query: string (可选) — 公众号名或 fakeid（search/subscribe/unsubscribe/fetch_all_history 时筛选）
